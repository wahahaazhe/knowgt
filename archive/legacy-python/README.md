# Legacy Python Experiments

These files are early experiments for a Python-based GitHub Trending analyzer.

The current workflow does not depend on them. Use WebFetch/WebSearch to read GitHub Trending, compare markdown history under `trending_reports/`, and write a new briefing.

Keep these files only as reference material unless the project later needs a deterministic command-line implementation.
